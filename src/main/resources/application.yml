# Common Properties
server:
  port: 8080
  servlet:
    context-path: /v1.0/h2boot

spring:
  datasource:
    url: jdbc:h2:mem:mydb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enabled: true
      path: /h2-console

# Development Profile.
---
spring:
  profiles: development

server:
  port: 8080
